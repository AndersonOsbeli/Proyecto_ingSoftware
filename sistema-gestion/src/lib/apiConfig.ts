/**
 * Configuración global de la API REST para el Frontend
 */
export const API_BASE_URL = 'http://localhost:5000/api';

/**
 * Indicador de uso de Backend SQL Server vs LocalStorage
 * Cambiar a true cuando ejecutes `npm start` en la carpeta /backend
 */
export const USE_BACKEND_API = true;
